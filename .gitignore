# ===============================================================
# .gitignore for Java, Gradle, IntelliJ, VS Code Projects
# ===============================================================

# 1. 빌드 결과물 (Build artifacts)
# ---------------------------------------------------------------
# 컴파일된 클래스 파일, JAR 파일 등이 저장되는 폴더들입니다.
# 이 폴더들은 소스코드가 아니므로 Git에 포함하지 않습니다.
build/
out/


# 2. Gradle 관련 파일 (Gradle files)
# ---------------------------------------------------------------
# Gradle이 프로젝트별로 사용하는 캐시, 래퍼 파일 등입니다.
# 다른 환경에서는 새로 받아야 하므로 Git에 포함하지 않습니다.
.gradle/
!gradle/wrapper/gradle-wrapper.jar # Gradle 버전 통일을 위해 wrapper jar 파일은 예외적으로 포함


# 3. 개발 도구(IDE) 설정 파일
# ---------------------------------------------------------------
# IntelliJ, VS Code 등은 프로젝트마다 개인 설정 파일을 만듭니다.
# 이 파일들은 개인의 개발 환경에 따라 달라지므로 공유하지 않습니다.

# IntelliJ IDEA
.idea/
*.iml
*.iws
*.ipr

# Visual Studio Code
.vscode/

# Eclipse / STS
.settings/
.classpath
.project


# 4. 운영체제 및 기타 파일 (OS generated files)
# ---------------------------------------------------------------
# macOS나 Windows가 자동으로 생성하는 시스템 파일들입니다.
.DS_Store
Thumbs.db


# 5. 로그 파일 (Log files)
# ---------------------------------------------------------------
# 애플리케이션 실행 시 생성되는 로그 파일들입니다.
logs/
*.log